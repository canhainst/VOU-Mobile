package com.example.vou_mobile.LoginMethod

class FacebookLogin : LoginMethod {
    override fun login(callback: (Boolean, String?) -> Unit) {
        // Implement logic đăng nhập bằng Facebook
        // Gọi callback khi hoàn tất
    }
}
